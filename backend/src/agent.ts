/**
 * Represents an agent with metadata for discovery and filtering.
 */

/**
 * Represents an agent with metadata for discovery and filtering.
 */
export type Agent = {
  id: number;
  name: string;
  description: string;
  tags: string[];
};
